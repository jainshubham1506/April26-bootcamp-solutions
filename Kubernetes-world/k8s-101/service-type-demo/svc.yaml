# # Nodeport
# apiVersion: v1
# kind: Service
# metadata:
#   name: my-service
  
# spec:
#   type: NodePort
#   selector:
#     app: nginx
#   ports:
#     - protocol: TCP
#       port: 8080
#       targetPort: 80
#       nodePort: 30080


# LoadBalancer
apiVersion: v1
kind: Service
metadata:
  name: my-service
  
spec:
  type: LoadBalancer
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
